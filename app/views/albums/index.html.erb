<h1>Portfolio</h1>
<p>Find me in app/views/albums/index.html.erb</p>

<div class="grid">
  <%= @albums.each do |album| %>
    <div>
      <%= cl_image_tag album.cover.key, height: 300, width: 400, crop: :fill %>
    </div>
  <% end %>
</div>

